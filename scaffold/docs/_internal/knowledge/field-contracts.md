# Frontend Field Contracts

> Derived field contracts: types, invariants, null semantics, and gotchas for every computed field in the frontend.
>
> **Quick lookup:** See `field-contracts-index.md` for one-line summaries.
> **Convention:** See `CONVENTIONS.md` for knowledge file rules.

---

<!-- Template for new entries:

### FIELD-XX: [field_name]
- **Type:** `string | number | boolean | null` (exact TS type)
- **Computed by:** [which engine/hook — describe functionally]
- **Consumed by:** [which views/components — describe functionally]
- **Invariants:** [what's always true about this field]
- **Null semantics:** [what null/undefined means — "not computed", "not applicable", "missing data"]
- **Gotchas:** [common mistakes when consuming this field]
- **Changed:** [date] — [what changed]
-->
