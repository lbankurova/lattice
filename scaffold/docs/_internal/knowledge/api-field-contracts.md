# Backend API Field Contracts

> Computed fields crossing the backend→frontend boundary. Each entry documents the exact type, nullability, and semantics of API response fields.
>
> **Convention:** See `CONVENTIONS.md` for knowledge file rules.

---

<!-- Template for new entries:

### BFIELD-XX: [field_name]
- **Endpoint:** `GET /api/...`
- **Type:** `string | number | boolean | null` (exact JSON type)
- **Nullable:** Yes/No
- **Enum values:** [list, if applicable]
- **Invariants:** [what's always true]
- **Semantics:** [what this field represents]
- **Changed:** [date] — [what changed]
-->
