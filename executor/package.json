{
  "name": "@lattice/executor",
  "version": "0.1.0",
  "description": "DAG workflow executor for Lattice development framework",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "lattice": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/cli.js",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  }
}
